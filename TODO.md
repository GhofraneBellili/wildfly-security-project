# TODO: Launch WildFly and Deploy IAM Service

- [ ] Start WildFly server using standalone.bat
- [ ] Deploy IAM application using Maven (mvn clean install wildfly:deploy)
- [ ] Verify deployment by checking server logs and accessing application endpoints
- [ ] Test with sample business app (run Node.js app and check integration)
